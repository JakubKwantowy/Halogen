0 0 0 "0 0 0 ""?"" :p1 top 1 swap - ; :p2 top 2 swap - ; ( p1 grab swap poke p1 p1 grab 1 + place ) p1 0 place p1 grab peek ( dup 63 = dup ( p2 0 place p2 grab peek ( dup 34 = ( pop 34 putc 0 ) pop putc p2 p2 grab 1 + place p2 grab peek ) pop 0 ) pop ~ ( pop putc 0 ) pop p1 p1 grab 1 + place p1 grab peek )" :p1 top 1 swap - ; :p2 top 2 swap - ; ( p1 grab swap poke p1 p1 grab 1 + place ) p1 0 place p1 grab peek ( dup 63 = dup ( p2 0 place p2 grab peek ( dup 34 = ( pop 34 putc 0 ) pop putc p2 p2 grab 1 + place p2 grab peek ) pop 0 ) pop ~ ( pop putc 0 ) pop p1 p1 grab 1 + place p1 grab peek )